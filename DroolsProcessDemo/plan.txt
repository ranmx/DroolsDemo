2017/09/22
1. 线上数据集的压力测试
2. kSession的原理
3. Plan B 规则引擎：内嵌UDF   【内嵌是不行的
4. 如果ksession不work会是怎么情况？是否可以添加一个sql的接口  【可以加在ksession外，意思不大

5. A表到B表多字段查询   【没有办法在process中分开。目前在找更直观的编辑界面
6. 如何结合现有UDF？  【可以随时将数据转换为Dataset并应用UDF。推荐进入规则引擎以前进行处理
7. 行为表和basic数据表分开处理？  【基础数据----> 主数据+RFM模型 -----> 静态属性标签+动态行为标签
8. 不同表之间的作业？A-->B-->C这样的有向图该如何处理？执行顺序如何确定和定义？  【可以注册为不同的process，放在不同的文件夹然后进行处理