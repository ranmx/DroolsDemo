//created on: 2017-10-24
package com.fosun

//list any import classes here.

import com.fosun.tableObjects.*;

//declare any global variables here

rule "get name if exists"
ruleflow-group "Group11"

    when
        m : YongAn(name != null && m.cleanedPerson.getReal_name() == null)
    then
        m.cleanedPerson.setReal_name(m.name);
end
