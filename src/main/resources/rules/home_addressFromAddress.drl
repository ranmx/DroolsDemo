//created on: 2017-10-24
package com.fosun

//list any import classes here.

import com.fosun.tableObjects.*;

//declare any global variables here

rule "get home address if exists"
ruleflow-group "Group11"

    when
         m : YongAn(address != null && m.cleanedPerson.getHome_address() == null)
    then
        m.cleanedPerson.setHome_address(m.address);
end

